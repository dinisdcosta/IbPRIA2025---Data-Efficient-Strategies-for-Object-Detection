train: run/train # Path to training data
val: run/val # Path to validation data
test: test # Path to test data
nc: 1 # Number of classes in the dataset

names: ['Whitefly'] # Class names in the dataset, here 'WF' is the only class
# Note: The paths are relative to the location of this file.